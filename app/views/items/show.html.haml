%div{class: "item item-#{@item.id}" }
  %h1= @item.name
  %p= @item.description
  %h3= @item.price
  %h4= @item.status
  = button_to "Add to Cart", cart_path(item_id: @item.id), :class => "btn btn_primary"
  = link_to 'Edit', edit_admin_item_path(@item) if current_admin?
